# Use emacs (default) keybindings
bindkey -e

# FZF find dirs
bindkey -s '^n' '^e^ucd "$(dirname "$(fzf)")"\n'

# tmux-sessionizer
bindkey -s '^f' '^e^u$HOME/.local/hmbin/tmux-sessionizer\n'

# Navigate with lf
bindkey -s '^j' '^e^ulfcd'
